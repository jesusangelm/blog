<% @posts.each do |p| %>
  <div class="post_wrapper">
    <h2 class="title"><%= link_to p.title, p %></h2>
    <p class="date"><%= p.created_at.strftime("%d %B, %Y") %> |
      <%= p.comments.count %> Comentarios
    </p>
  </div>
<% end %>
